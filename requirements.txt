# Watson dependencies
fastapi>=0.100.0
uvicorn>=0.23.0
aiohttp>=3.8.0
pydantic>=2.0.0
jinja2>=3.1.0
python-multipart>=0.0.6

# Optional: for agent adapters
# hermes (local) — install Hermes Agent separately
# openclaw — install OpenClaw separately

# Dev
pytest>=7.0.0
pytest-asyncio>=0.21.0
